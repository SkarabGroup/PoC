# ─────────────────────────────────────────────────────────
# Code Guardian – Environment Variables
# ─────────────────────────────────────────────────────────
# Copy this file to .env and fill in the values.
#
# When using Docker (./start-all.sh), MongoDB and Redis
# connection strings are set automatically via
# docker-compose.yml — you don't need to change them here.
# ─────────────────────────────────────────────────────────

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION=15m
JWT_REFRESH_SECRET=your-refresh-secret-key
JWT_REFRESH_EXPIRATION=7d

# AWS Bedrock (only needed if USE_MOCK_ANALYSIS=false)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_SESSION_TOKEN=your-aws-session-token
AWS_REGION=eu-central-1
ORCHESTRATOR_BEDROCK_MODEL_ID=arn:aws:bedrock:eu-central-1::inference-profile/eu.amazon.nova-lite-v1:0
MAX_AGENT_ITERATIONS=20

# Analysis Mode
# Set to "true" to use the mock orchestrator (no AWS credentials needed)
USE_MOCK_ANALYSIS=true
